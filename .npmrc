# Enable postinstall scripts for packages like Supabase CLI
# that need to download platform-specific binaries
enable-pre-post-scripts=true

# Use hoisted node_modules for better compatibility
node-linker=hoisted
