# Enable postinstall scripts for packages that need to
# download platform-specific binaries or install hooks
enable-pre-post-scripts=true

# Use hoisted node_modules for better compatibility
node-linker=hoisted
